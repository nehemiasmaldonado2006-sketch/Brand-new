import { ComingSoon } from "@/components/ComingSoon";

export default function CmaLibraryPage() {
  return (
    <ComingSoon
      title="Marketing Production — CMA"
      note="The CMA library, comps builder and Canva-style editor are being built next, after the live dashboard, Email Desk, Calendar, Rate Desk and Market Desk."
    />
  );
}
